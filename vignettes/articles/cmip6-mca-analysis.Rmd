---
title: "Rerunning the Montana Climate Assessment with CMIP6 Data"
---

```{r include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  eval = nzchar(Sys.getenv("CMIP6_DIR"))
)
```

```{r setup}
library(magrittr)
library(terra)
library(sf)
library(ggplot2)

mt_state_plane <- 'PROJCS["NAD_1983_HARN_StatePlane_Montana_FIPS_2500 (deprecated)",
    GEOGCS["NAD83(HARN)",
        DATUM["NAD83_High_Accuracy_Reference_Network",
            SPHEROID["GRS 1980",6378137,298.257222101,
                AUTHORITY["EPSG","7019"]],
            AUTHORITY["EPSG","6152"]],
        PRIMEM["Greenwich",0,
            AUTHORITY["EPSG","8901"]],
        UNIT["degree",0.0174532925199433,
            AUTHORITY["EPSG","9122"]],
        AUTHORITY["EPSG","4152"]],
    PROJECTION["Lambert_Conformal_Conic_2SP"],
    PARAMETER["latitude_of_origin",44.25],
    PARAMETER["central_meridian",-109.5],
    PARAMETER["standard_parallel_1",45],
    PARAMETER["standard_parallel_2",49],
    PARAMETER["false_easting",600000],
    PARAMETER["false_northing",0],
    UNIT["metre",1,
        AUTHORITY["EPSG","9001"]],
    AXIS["Easting",EAST],
    AXIS["Northing",NORTH],
    AUTHORITY["ESRI","102300"]]'

mt <- sf::read_sf("https://data.climate.umt.edu/mca/fgb/mt.fgb") %>% 
  sf::st_transform(mt_state_plane)

counties <- sf::read_sf(
  "https://data.climate.umt.edu/mca/fgb/mt_counties.fgb"
) %>%
  sf::st_transform(mt_state_plane)

climdiv <- sf::read_sf(
  "https://data.climate.umt.edu/mca/fgb/mt_climdiv.fgb"
) %>%
  sf::st_transform(mt_state_plane)


reference_period <- c(1981, 2010)
mid_century <- c(2040, 2069)
end_century <- c(2070, 2099)
```

```{r to_shp} 
mco_mask <- function(rast, mask){
  raster::mask(rast,
               fasterize::fasterize(mask %>%
                                      sf::st_transform(raster::projection(rast)),
                                    raster::raster(rast))
  )
}

to_shp <- function(x, mt) {
  
  tmp <- x %>%
    terra::project(mt_state_plane) %>%
    # terra::mask(mt %>% terra::vect(), touches=F) %>%
    raster::stack() %>%
    # mco_mask(mask = mt) %>%
    spex::qm_rasterToPolygons(na.rm = T) %>%
    tidyr::pivot_longer(-geometry, names_to = "time")  %>%
    sf::`st_crs<-`(mt_state_plane)
  
  mt_use <- sf::st_transform(mt, sf::st_crs(tmp)) %>% 
    dplyr::select(geometry)
  
  sf::st_intersection(tmp, mt_use) 
}
```

```{r plot_seasonal} 
plot_seasonal <- function(dat, mt) {
  
  sf::st_as_sf(dat) %>% 
  ggplot() + 
    geom_sf(aes(fill = value), color = NA) + 
    facet_wrap(~time) + 
    geom_sf(data = mt, fill = NA) + 
    theme_bw()
}

plot_timeseries <- function(dat) {
  dat %>%
    dplyr::mutate(
      variable = dplyr::recode(
        variable,
        "tas" = "Average Temperature",
        "tasmax" = "Avg. Max Temperature",
        "tasmin" = "Avg. Min Temperature",
        "pr" = "Total Precipitation"
      ), 
      year = as.numeric(year)
    ) %>%
    dplyr::filter(time != "Annual") %>% 
    dplyr::mutate(time = factor(time, levels = c("Winter", "Spring", "Summer", "Fall"))) %>% 
    ggplot(aes(x=year, y=value, color = variable)) + 
      geom_line() + 
      geom_smooth(method = "lm") +
      facet_wrap(~time, scales = "free_y") +
      theme_minimal() + 
      labs(x="", y = "Temperature (°F)", color="")
}
```

```{r summary_functions}
time_avg <- function(
    fs, reference_period, period="annual", fun="mean", transform = NULL, remote = FALSE
) {
  
  checkmate::assert_choice(period, c("annual", "seasonal", "monthly"))
  
  read_func <- ifelse(remote, read_from_server, terra::rast)
  print(fs[1])
  stk <- read_func(fs) %>%  
    terra::tapp(index="days", fun="mean")
  
  times <- tibble::tibble(
    date = names(stk) %>% 
      stringr::str_replace("X", "") %>% 
      lubridate::as_date()
  ) %>% 
    dplyr::mutate(
      season = dplyr::case_when(
        lubridate::month(date) %in% c(12, 1, 2) ~ "Winter", # 1=winter
        lubridate::month(date) %in% c(3, 4, 5) ~ "Spring", # 2=spring
        lubridate::month(date) %in% c(6, 7, 8) ~ "Summer", # 3=summer
        lubridate::month(date) %in% c(9, 10, 11) ~ "Fall" # 4=fall
      ), 
      idx = 1:dplyr::n(),
      month = lubridate::month(date)
    ) %>% 
    dplyr::rowwise() %>% 
    dplyr::mutate(
      year = ifelse(
        (season == "Winter") && (lubridate::month(date)) == 12, 
        lubridate::year(date) + 1,
        lubridate::year(date)
      )
    ) %>%
    dplyr::group_by(year, season) %>% 
    dplyr::mutate(
      yearly_grp = dplyr::cur_group_id(),
      group_count = dplyr::n()
    ) %>% 
    dplyr::filter(
      year %in% reference_period[1]:reference_period[2],
    ) %>% 
    dplyr::mutate(
      yearly_grp = glue::glue("{season} {year}")
    )
  
  if (nrow(times) == 0) {
    return(NULL)
  }


  if (period == "annual"){
    r_filtered <- terra::subset(stk, times$idx) 

    out <- terra::tapp(
      r_filtered, index = times$year, fun = fun
    )
  } else if (period == "seasonal") {
    
    time_tmp <- dplyr::filter(times, group_count == 3)
    r_filtered <- terra::subset(stk, time_tmp$idx) 

    out <- terra::tapp(
      r_filtered, index = time_tmp$yearly_grp, fun = fun
    )  
  } else {
    r_filtered <- terra::subset(stk, times$idx) 

    out <- r_filtered
  }
  
  if (!is.null(transform)) {
    return(transform(out))
  }
  return(out)
}

timeseries_to_summary <- function(r, period) {
  
  if (is.null(r)) {
    return(NULL)
  }
  checkmate::assert_choice(period, c("annual", "seasonal", "monthly"))
  
  meta <- tibble::tibble(name = names(r))
  
  if (period == "annual") {
    grp <- rep("Annual", terra::nlyr(r))
  } else if (period == "seasonal") {
    grp <- meta %>% 
      tidyr::separate(name, c("season", "year")) %>% 
      dplyr::mutate(season = factor(season, levels = c("Winter", "Spring", "Summer", "Fall"))) %$%
      season
  } else {
    grp <- meta %>% 
      dplyr::transmute(
        date = lubridate::as_date(name, format="X%Y.%m.%d"),
        month = month.name[lubridate::month(date)]
      ) %$%
      month
  }
  
  terra::tapp(
    r, index = grp, fun = "mean"
  )
}

filter_files <- function(f_list, scenario, variable) {
  grep(".json", f_list, value = T, invert = T) %>% 
    grep(scenario, ., value = T) %>% 
    grep(variable, ., value = T)
}

get_global_summary <- function(r, fun = "mean") {
  
  terra::global(r, fun, na.rm = T) %>%
    tibble::rownames_to_column() %>% 
    magrittr::set_names(c("time", "value")) %>% 
    tibble::as_tibble() 
}

trend_func <- function(ys, threshold = 0.05) {
  
  xs <- 1:length(ys)
  mod <- lm(ys ~ xs)
  coeffs <- coef(mod)
  
  meta <- summary(mod)
  if (meta$coefficients[,4][2] > threshold) {
    return(0)
  }
  coeffs[2]
}



tas_tsfm <- function(x) (x - 273.15) * 1.8 + 32
pr_tsfm <- function(x) x/25.4

```

```{r preprocess_cmip, warning=FALSE}
f_list <- RCurl::getURL(
  "https://data.climate.umt.edu/mca/cmip/",
  ftp.use.epsv = TRUE,
  dirlistonly = FALSE
) %>% 
  XML::getHTMLLinks() %>% 
  grep(".tif", ., value = T) %>%
  grep(".json", ., value = T, invert = T) 

cmip_files <- file.path(
  "https://data.climate.umt.edu/mca/cmip",
  basename(f_list)
)
```


```{r table2.1}
dat <- c(
  "https://data.climate.umt.edu/mca/gridmet/tmmn.tif", 
  "https://data.climate.umt.edu/mca/gridmet/tmmx.tif"
) %>% 
  purrr::map(function(x) {
    time_avg(
      x,
      c(1991, 2020),
      "seasonal", 
      "mean", 
      tas_tsfm,
      TRUE
    ) %>% 
      timeseries_to_summary("seasonal") %>%
      spat_summary(shp = climdiv, "NAME", fun="mean") %>% 
      dplyr::select(div=NAME, timescale, value) %>% 
      dplyr::mutate(
        variable = basename(tools::file_path_sans_ext(x))
      )
  }) %>%
  dplyr::bind_rows() %>% 
  tidyr::pivot_wider(names_from = "variable", values_from = "value") %>% 
  dplyr::mutate(
    tavg = (tmmn + tmmx)/2,
  ) 

annual <- tidyr::pivot_longer(dat, c(tmmn, tmmx, tavg)) %>% 
  dplyr::group_by(div, name) %>% 
  dplyr::summarise(Annual = mean(value), .groups="drop") %>% 
  tidyr::pivot_wider(names_from = name, values_from = Annual) %>% 
  dplyr::mutate(timescale = "Annual")

dplyr::bind_rows(dat, annual) %>% 
  dplyr::transmute(
    `Climate Division` = tools::toTitleCase(tolower(div)), 
    timescale = factor(timescale, levels = c("Annual", "Winter", "Spring", "Summer", "Fall")),
    meta = paste(round(tmmn, 1), round(tavg, 1), round(tmmx, 1), sep = " / ")
  ) %>%
  tidyr::pivot_wider(names_from = 'timescale', values_from='meta', names_sort = T) 
  # knitr::kable(
  #   caption = "Table 1: Average temperatures (°F) and Precipitation (in) across Montana from 1991-2020."
  # )
```
```{r table2.2}
dat <- time_avg(
  "https://data.climate.umt.edu/mca/gridmet/pr.tif",
  c(1991, 2020),
  "seasonal", 
  "sum", 
  pr_tsfm,
  TRUE
) %>% 
  timeseries_to_summary("seasonal") %>%
  spat_summary(shp = climdiv, "NAME", fun="mean") %>% 
  dplyr::select(div=NAME, timescale, value) 

annual <- dplyr::group_by(dat, div) %>% 
  dplyr::summarise(value = sum(value), .groups="drop") %>% 
  dplyr::mutate(timescale = "Annual")

dplyr::bind_rows(dat, annual) %>% 
  dplyr::transmute(
    `Climate Division` = tools::toTitleCase(tolower(div)), 
    timescale = factor(timescale, levels = c("Annual", "Winter", "Spring", "Summer", "Fall")),
    value = value
  ) %>%
  tidyr::pivot_wider(names_from = 'timescale', values_from='value', names_sort = T) 
```
```{r table 2.3}
dat <- c(
  "https://data.climate.umt.edu/mca/gridmet/tmmn.tif", 
  "https://data.climate.umt.edu/mca/gridmet/tmmx.tif"
) %>% 
  purrr::map(function(x) {
    time_avg(
      x,
      c(1979, 2020),
      "seasonal", 
      "mean", 
      tas_tsfm,
      TRUE
    ) %>% 
      spat_summary(
        shp = climdiv, attr_id = "NAME", fun="mean"
      ) %>% 
      dplyr::select(div=NAME, timescale, value) %>% 
      dplyr::mutate(
        variable = basename(tools::file_path_sans_ext(x))
      )
  }) %>% 
  dplyr::bind_rows()


# # Just a check to look at the trends. 
# dat %>% 
#   tidyr::separate(timescale, c("season", "year")) %>%
#   dplyr::mutate(year = as.numeric(year)) %>% 
#   dplyr::filter(variable == 'tmmn') %>%
#   ggplot(aes(x=year, y=value)) + 
#     geom_point() +
#     facet_wrap(season~div, scales="free_y") +
#     geom_smooth(method='lm')

tidyr::separate(dat, timescale, c("season", "year")) %>%
  dplyr::group_by(div, year, variable) %>%
  dplyr::mutate(Annual = mean(value)) %>% 
  dplyr::ungroup() %>% 
  tidyr::pivot_wider(names_from = season, values_from = value) %>% 
  tidyr::pivot_longer(-c(div, year, variable), names_to = "season") %>%
  tidyr::pivot_wider(names_from = variable, values_from = value) %>% 
  dplyr::mutate(tavg = (tmmn + tmmx)/2) %>% 
  tidyr::pivot_longer(-c(div, year, season)) %>%
  dplyr::group_by(div, season, name) %>% 
  dplyr::summarise(trend = trend_func(value, threshold=0.05)) %>%
  dplyr::mutate(trend = round(trend, 3)) %>% 
  tidyr::pivot_wider(names_from = name, values_from = trend) %>% 
  dplyr::ungroup() %>% 
  dplyr::transmute(
    `Climate Division` = tools::toTitleCase(tolower(div)), 
    timescale = factor(season, levels = c("Annual", "Winter", "Spring", "Summer", "Fall")),
    meta = paste(tmmn, tavg, tmmx, sep = " / ")
  ) %>%
  tidyr::pivot_wider(names_from = 'timescale', values_from='meta', names_sort = T) 
```

```{r fig2.4, message=FALSE, warning=FALSE}
dat <- c(
  "https://data.climate.umt.edu/mca/gridmet/tmmn.tif", 
  "https://data.climate.umt.edu/mca/gridmet/tmmx.tif"
) %>% 
  purrr::map(function(x) {
    time_avg(
      x,
      c(1979, 2020),
      "annual", 
      "mean", 
      tas_tsfm,
      TRUE
    ) %>% 
      spat_summary(
        shp = climdiv, attr_id = "NAME", fun="mean"
      ) %>% 
      dplyr::select(div=NAME, timescale, value) %>% 
      dplyr::mutate(
        variable = basename(tools::file_path_sans_ext(x))
      )
  }) %>% 
  dplyr::bind_rows()

dat %>% 
  dplyr::mutate(
    div = tools::toTitleCase(tolower(div)),
    year = timescale %>% 
      stringr::str_replace("X", "") %>% 
      as.numeric()
  ) %>% 
  dplyr::group_by(div, year) %>% 
  dplyr::summarise(value = mean(value)) %>%
  ggplot(aes(x=year, y=value)) +
    geom_line() + 
    geom_smooth(method = "lm") + 
    facet_wrap(~div)
```


```{r table2.4}
dat <- time_avg(
  "https://data.climate.umt.edu/mca/gridmet/pr.tif",
  c(1979, 2020),
  "seasonal", 
  "sum", 
  pr_tsfm,
  TRUE
) %>% 
  spat_summary(
    shp = climdiv, attr_id = "NAME", fun="mean"
  ) %>% 
  dplyr::select(div=NAME, timescale, value)



# # Just a check to look at the trends. 
# dat %>% 
#   tidyr::separate(timescale, c("season", "year")) %>%
#   dplyr::mutate(year = as.numeric(year)) %>% 
#   dplyr::filter(variable == 'tmmn') %>%
#   ggplot(aes(x=year, y=value)) + 
#     geom_point() +
#     facet_wrap(season~div, scales="free_y") +
#     geom_smooth(method='lm')

tidyr::separate(dat, timescale, c("season", "year")) %>%
  dplyr::group_by(div, year) %>%
  dplyr::mutate(Annual = sum(value)) %>% 
  dplyr::ungroup() %>% 
  tidyr::pivot_wider(names_from = season, values_from = value) %>% 
  tidyr::pivot_longer(-c(div, year), names_to = "season") %>%
  dplyr::group_by(div, season) %>% 
  dplyr::summarise(trend = trend_func(value, threshold=0.05)) %>%
  dplyr::mutate(trend = round(trend, 3)) %>% 
  dplyr::ungroup() %>% 
  dplyr::transmute(
    `Climate Division` = tools::toTitleCase(tolower(div)), 
    timescale = factor(season, levels = c("Annual", "Winter", "Spring", "Summer", "Fall")),
    meta = trend
  ) %>%
  tidyr::pivot_wider(names_from = timescale, values_from = meta) 
```

```{r difference_functions, message=FALSE, warning=FALSE}
filter_and_read <- function(pattern, historical, period, fun="mean", zonal=TRUE, timescale="annual") {
  grep(pattern, cmip_files, value = T) %>%
  grep("historical", ., value = T, invert=!historical) %>%
  purrr::map(function(x) {
      tmp <- time_avg(
        x,
        period,
        timescale, 
        fun, 
        tas_tsfm,
        TRUE
      ) 
      
      if (!zonal) {
        tmp <- timeseries_to_summary(tmp, timescale)
        out <- tibble::tibble(f = basename(x)) %>%
          tidyr::separate(
            f, c("model", "scenario", "run", "variable"), sep = "_"
          ) %>%
          dplyr::mutate(
            period = paste(period, collapse = "-"),
            r = list(tmp)
          )
        return(out)
      }
      
      out <- spat_summary(
        tmp, shp = climdiv, attr_id = "NAME", fun="mean"
      ) %>% 
        dplyr::select(div = NAME, year = timescale, value) %>% 
        dplyr::mutate(f = basename(x) %>% tools::file_path_sans_ext())
      
      return(out)
  })
}

isolate_models <- function(pattern, historical, period, fun="mean", timescale="annual") {
  filter_and_read(pattern, historical, period, fun, TRUE, timescale) %>% 
  dplyr::bind_rows() %>% 
  dplyr::mutate(
    div = tolower(div),
    year = stringr::str_replace(year, "X", "") %>% as.numeric()
  ) %>% 
  tidyr::separate(f, c("model", "scenario", "drop1", "drop2"), sep = "_") %>% 
  dplyr::select(-dplyr::starts_with("drop")) %>%
  dplyr::mutate(period = paste(period, collapse="-"))
}

future_boxplot <- function(dat) {
  ggplot(dat, aes(x=scenario, y=diff, fill=scenario)) + 
    geom_boxplot(alpha=0.2) + 
    facet_wrap(div~period, nrow=2) + 
    theme_minimal()
}

```

```{r fig2.9, message=FALSE, warning=FALSE}
dat <- dplyr::bind_rows(
  isolate_models("tas.tif", TRUE, c(1981, 2010), "mean", timescale="annual"),
  isolate_models("tas.tif", FALSE, c(2049, 2069), "mean", timescale="annual"),
  isolate_models("tas.tif", FALSE, c(2070, 2099), "mean", timescale="annual")
)

dat %>% 
  dplyr::group_by(div, model, scenario, period) %>% 
  dplyr::summarise(value = mean(value), .groups = "drop") %>%
  dplyr::left_join(
    dplyr::filter(., scenario == "historical") %>% 
        dplyr::rename(historical = value) %>% 
        dplyr::select(-scenario, -period)
    ,
    by = c("div", "model")
  ) %>% 
  dplyr::filter(scenario != "historical") %>% 
  dplyr::mutate(diff = value - historical) %>%
  future_boxplot()
```


```{r fig2.10, warning=FALSE}
dat <- dplyr::bind_rows(
  filter_and_read("tas.tif", TRUE, c(1981, 2010), "mean", FALSE),
  filter_and_read("tas.tif", FALSE, c(2049, 2069), "mean", FALSE),
  filter_and_read("tas.tif", FALSE, c(2070, 2099), "mean", FALSE) 
) 

test <- dat %>% 
  dplyr::left_join(
    dplyr::filter(., scenario == "historical") %>% 
        dplyr::rename(historical = r) %>% 
        dplyr::select(-scenario, -period)
    ,
    by = c("model", "run", "variable")
  ) %>%
  dplyr::filter(scenario != "historical") %>%
  dplyr::rowwise() %>%
  dplyr::mutate(
    diff = list(r - historical),
  ) %>%
  dplyr::group_by(scenario, period) %>% 
  dplyr::summarise(
    diff = list(terra::app(terra::rast(diff), fun="mean"))
  ) %>%
  dplyr::rowwise() %>% 
  dplyr::mutate(
    diff = list(to_shp(diff, mt=mt))
  ) %>%
  dplyr::select(scenario, period, diff) %>% 
  tidyr::unnest(diff) %>% 
  sf::st_as_sf()
  
ggplot(test %>% dplyr::filter(scenario == 'ssp126')) + 
    geom_sf(aes(fill=value), color=NA) + 
    geom_sf(aes(), data = climdiv, fill=NA) + 
    facet_wrap(~period) + 
    theme_minimal()
```
